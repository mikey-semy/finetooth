# Дневник ревью

Пишется по ходу работы: что решили, почему, что сознательно не делали. Находки
восстановимы повторным прогоном — решения нет.

Добавлять только через `python3 scripts/review/review.py log <БЛОК> "текст"`.

- **2026-09-15** · `setup` — Фаза 0 собрана: 29 блоков, инструмент `scripts/review/review.py`, шаблоны промптов, карта покрытия (1819 файлов, непокрытых нет). Порядок блоков: сначала сквозные инварианты, затем вертикальные срезы, затем то, что требует живого стенда.
- **2026-09-15** · `setup` — Решено, что блоки фазы 3 (E1–E4) не владеют файлами: они работают на запущенной системе. Первая версия инструмента из-за этого считала, что они покрывают весь репозиторий, и покрытие сошлось фиктивно; поведение исправлено — пустой список путей означает пустое множество, а не «всё».
- **2026-09-15** · `setup` — При сверке покрытия обнаружилось, что микросервис `apps/office-preview` не был учтён ни в одном блоке: он отдан блоку V3 вместе с остальным путём файла.
- **2026-09-15T19:19:31Z** · `setup` — Сверка шаблонов путей выявила, что карточка заявки собрана из route-local компонентов, и каждый профильный блок не увидел бы свой кусок интерфейса: комментарии достались бы списку заявок, карточка аварии — тоже. Владение переназначено по файлам, а проверка мёртвых шаблонов добавлена в review-check, чтобы опечатка в пути больше не сужала блок молча.
- **2026-09-15T19:52:07Z** · `H1` — Блок принят. Охотник прочитал 25/25 файлов, обе таблицы приёмки сданы; верификатор сверил таблицу маршрутов механически (322 регистрации против 321 у охотника — разница в двух ветках GET /ws, объявленных схлопнутыми) и подтвердил колонку «Гейт» во всех строках. Гранты «миграция ↔ сид» проверены дважды: 48 инструкций INSERT INTO role_permissions переписаны в read-only запросы на живой dev-БД, расхождений нет — класс дефекта, из-за которого терялись inspections:*, закрыт.
