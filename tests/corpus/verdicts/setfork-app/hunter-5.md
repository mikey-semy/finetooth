## Hypotheses
(…)
- `H5.1 — проверена: каждый путь расхода открыт и размечен (таблица выше). Денежная квота
(…)
- `H5.2 — проверена: атомарности нет нигде — `aiQuota`/`freeGenQuota`/`listQuota` читают
(…)
- `H5.3 — проверена: месячное окно считается базой (`date_trunc('month', now())`) и расход
(…)
- `H5.4 — проверена: суточный кап `date_trunc('day', now())` — те же часы базы, что и у
(…)
- `H5.5 — проверена: при недоступном эндпоинте кредитов система не уходит в ПЛАТНЫЙ цикл —
(…)
- `H5.6 — неприменима в части срока: подписки и срока в коде не существует, `planFor` —
(…)
- `H5.7 — проверена: открыты ВСЕ 16 вызовов `listQuota`/`aiQuota`/`freeGenQuota`. Ник нигде
(…)
- `H5.8 — проверена: `listQuota` — `count(*)` по `ownerId` без единого фильтра
(…)
- `H5.9 — проверена: перечислены все места применения фикс-окна. К ПЛАТНЫМ путям оно
(…)
- `H5.10 — проверена: `underTwoKeyRate` считает оба ключа всегда — `Promise.all`, а не
(…)
- `H5.11 — проверена: да, расход всегда пишется вне транзакции с операцией, и `recordUsage`
(…)
- `H5.12 — проверена частично, МУТАЦИЕЙ НЕ ПРОГНАНА. Прогон невозможен в отведённых рамках:
## Section
