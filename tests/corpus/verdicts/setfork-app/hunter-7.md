### Hypotheses
(…)
| hypothesis |
| 1 | перечень маршрутов записи и что каждый проверяет | **проверена** — таблица ниже, 27 маршрутов, 11 пустых клеток |
| 2 | тихая потеря полей на конвертации | **проверена, подтверждена трижды** — V1d-001, V1d-002, V1d-003 |
| 3 | сверка версии при записи | **проверена, подтверждена** — V1d-006, V1d-007, V1d-011 |
| 4 | запись версии мимо ядра (прямой SQL) | **проверена**: прямого SQL в `steps`/`template_versions` на пути СОДЕРЖИМОГО не осталось (ветка `replaceDraftStepsIn` удалена, гейт `tests/features/mcp/git-parity.itest.ts`). Мимо ядра пишутся только мета (V1d-005) и уровень проверки (V1d-009) |
| 5 | вычисляемое принято от клиента | **проверена, не подтвердилась** — `danger`/`needsHuman`/`level` приходят от АВТОРА осознанно (это не вычисляемые поля, а пометки), `danger` дополнительно выводится из команды в `toStepInput`; отпечаток содержимого и номер версии клиент не задаёт |
| 6 | публикация: черновик, отредактированный до публикации, минует часть проверок | **проверена, не подтвердилась** — `publishOwnedDrafts` держит владельца, статус, видимость, архив, заморозку и модерацию УСЛОВИЕМ В САМОМ `UPDATE`; см. «проверено и признано корректным» |
| 7 | форк даёт копию с более слабым гейтом | **проверена, подтверждена** — но у «Использовать как шаблон», а не у форка: V1d-010 |
| 8 | зеркало: отказ не виден ни пользователю, ни журналу | **проверена, не подтвердилась** — лестница пауз, свой счётчик при недоступном ядре, текст отказа по языку, отставание без ошибки, переполнение интервала: всё закрыто тестами `mirror-*` |
| 9 | удаление: каскад неполон | **проверена, не подтвердилась как новое**: все FK на `templates` несут `on delete cascade`/`set null`; git-репозиторий переживает удаление — но это ИЗВЕСТНО и оговорено в интерфейсе, что и стережёт `delete-promise.test.ts` |
| 10 | есть путь, меняющий уже опубликованную версию | **проверена, подтверждена частично** — `setVerificationLevel` меняет метаданные опубликованной версии, но это решение 0018 и оно пишет аудит; дефект в нём другой (V1d-009) |
### Section
(…)
  на пути содержимого не осталось — гипотеза 4 в этой части не подтвердилась.
