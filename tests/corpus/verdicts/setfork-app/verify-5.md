(…)
  кодом, ничего не выполнив. Гипотеза 11 в части инъекции — не подтвердилась.
(…)
- **Гипотеза 1 (узды на правило зависимостей нет) — опровергнута, проверил сам.**
## Hypotheses
(…)
| — |
| 1 | проверена — опровергнута | узда есть, но в другом месте: `eslint.config.mjs:307` + `default: 'disallow'` и ноль политик `from: {type: 'core'}`; `eslint-suppressions.json` по `src/core` пуст. Красной узду охотник не делал |
| 2 | проверена | выписаны все 14 рукописных копий предиката публичности поимённо; отличия, меняющего ответ, нет; дрейф на будущее уже записан в H1 |
| 3 | проверена — подтвердилась | Протокол 1: 53 строки через `findDestructive`/`findRisky`; находки H15-001, H15-003 |
| 4 | проверена | см. раздел про недетерминизм: находка H15-006 (`localeCompare` зависит от локали процесса), прогон в трёх локалях |
| 5 | проверена — дефекта нет | `entities.ts` сверен со схемой: расхождения, меняющего поведение, не найдено |
| 6 | проверена верификатором — подтвердилась | охотник до портов не дошёл; проверяющий сверил `ports.ts` с адаптерами: находка про `CollabStore` (теряет `blockId`, `danger`, «нужен человек») и находка про `AiPort.embed` (порт требует учёт стоимости, адаптер его выбрасывает). ⚠️ В реестре они получили номера H15-013 и H15-014: импорт раздаёт id по позиции в файле находок, и номера из текста отчёта с ними не совпадают — ссылаться надо на реестр |
| 7 | проверена — дефекта нет | `block-identity.ts`: стабильность `bid` подтверждена, дефекта не дал |
| 8 | проверена — подтвердилась | Протокол 3: 15 вариантов `quizContentHash` в трёх локалях; находки H15-004, H15-005 |
| 9 | проверена — опровергнута | Протокол 2: 18 форм `javascript:`/`data:`/`vbscript:` — все дали `null`; разбор доверен конструктору `URL`, решение по `u.protocol` |
| 10 | проверена — не подтвердилась | 8 URL через `applyAffiliate`: чужой хост метку не получает, граница по точке работает |
| 11 | проверена — подтвердилась частично | `carriesCommands` стережёт диалекты; инъекции из заголовка нет — дыра не подтвердилась (проверено прогоном через `bash` и `python3`) |
| 12 | проверена — дефекта нет | `product.ts` утверждает про товар ровно три вещи; схему URL не проверяет, но краевая защита стоит единым способом (`safeHref`, `/api/go` отвечает 404 на не-`https?`) |
