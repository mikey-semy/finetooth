(…)
- **Гипотеза 1 (правило зависимостей ядра никто не стережёт) — опровергнута.** В
(…)
- **`links.ts` против `javascript:`/`data:`/`vbscript:` (гипотеза 9) — держит.** Проверены
(…)
  конструктором `URL`, а не регуляркой (гипотеза 10 не подтвердилась). Перезапись чужого `tag`
(…)
- **Гипотезу 6 (порт обещает не то, что делает адаптер) проверил выборочно, а не сплошь.**
(…)
- **Гипотеза 4 проверена не полностью.** `Date.now()`, `Math.random()`, `process.env` в
