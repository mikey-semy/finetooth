### Hypotheses
(…)
| hypothesis |
| 1 | Обход каталога | **проверена, подтверждена** — H2-001. Обход воспроизведён запросом, файл вне каталога отдан целиком. Второй путь (`icons`) проверен и держит. |
| 2 | Имя контейнера в команде | **проверена, нарушения нет** — `execFile` массивом, оболочки нет, имя сверяется со списком фактов. Цитаты ниже. |
| 3 | Тип и размер иконки | **проверена, частично подтверждена** — тип закрыт `sharp`, размер проверяется слишком поздно (H2-004). |
| 4 | Расширение доверено запросу | **проверена, нарушения нет** — расширение всегда `.png`, `Content-Type` запроса не используется нигде. |
| 5 | `suggest` ходит наружу | **проверена, нарушения нет** — набор локальный, сети нет вовсе. Зато найдены два дефекта поиска (H2-007, H2-008). |
| 6 | Режим обдува не проверяется | **проверена** — слово проверяется списком и это правильно; дефекты рядом: пустое тело и отказ записи дают `500` с пустым телом (H2-005), общий временный файл даёт гонку (H2-006). |
| 7 | Ошибка наружу | **проверена, путей и вывода команд в ответах нет.** Но наружу уходит кое-что похуже — сам журнал контейнера (H2-002). |
## Section
(…)
**Слово режима проверяется списком (гипотеза 6).** `isMode` — белый список из
