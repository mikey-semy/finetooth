### Hypotheses
(…)
| hypothesis |
| 1 | файла нет | **живьём**: `ARCH_FACTS_DIR` на пустой каталог | главная честна («Фактов нет: сборщик panel ещё не отработал»), `/logs` и `/debts` врут → H3-007 |
| 2 | файл битый | **живьём**: `machine.json` обрезан на 6000 байт | неотличимо от «файла нет» → H3-002 |
| 3 | поле пропало | **механически** (сверка типов с диском) + **живьём** (варианты фактов) | `SiteCard` разошёлся целиком → H3-010; пропажа обязательного поля валит страницу → H3-003 |
| 4 | число пришло строкой или `null` | **живьём** + прямой прогон `tone()` | `NaN` красится в аварию → H3-001; `load` строками → 500 → H3-003 |
| 5 | возраст факта не используется | **механически** (grep `collected`) + **живьём** (термокартина трёхдневной давности) | читается только у `machine.json` → H3-004 |
| 6 | кэш Next | чтение директив | у всех шести страниц `export const dynamic = "force-dynamic"`, `readFile` идёт на каждый запрос — **корректно** |
| 7 | часовые пояса и даты | прямой прогон `parseRu`/`fromIsoDate`/`whenShort`/`money` | H3-009, H3-011, H3-012 |
| 8 | сортировка и «первый» | grep `[0]` + живьём | `steps[0][1]` в `fan-curve` валит страницу на пустой кривой → H3-003; ключ соединения `host` сегодня сходится — см. «признано корректным» |
### Section
