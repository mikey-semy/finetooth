### Hypotheses
(…)
| hypothesis |
| 1 | запись не атомарна | прочитаны все пять маршрутов; ни одного `tmp`+`rename` | **подтвердилась** → H1-002 |
| 2 | комментарии переживают не все пути | прогон: правка подписки через реальный файл с вставленным комментарием | **подтвердилась** → H1-007, H1-014 |
| 3 | две вкладки | код: ни блокировки, ни сверки mtime/ETag; `servers.yml` пишут ДВА маршрута | **подтвердилась** → H1-008 |
| 4 | сборщик читает в момент записи | прочитан `panel` (`yaml.safe_load` без `try`) и `subscription-facts` | **подтвердилась**, сведена в H1-002 |
| 5 | необъявленные поля исчезают | прогон `setField` по реальному `servers.yml` | частично: побайтовые пути сохраняют поля, но `panel` (вложенная карта) стирается → H1-003 |
| 6 | вход не проверяется | прогон разбора YAML для описаний агентов; чтение `clean()` | **подтвердилась** → H1-004, H1-006, H1-010, H1-011 |
| 7 | ключ записи выбирается по имени | код всех четырёх поисков | **подтвердилась** → H1-001, H1-017 |
| 8 | ответ врёт | код маршрутов и клиентов | **подтвердилась** → H1-009, H1-012 |
| 9 | правка не доезжает до git | `grep` по всему `src/` — ни одного вызова git | **подтвердилась** → H1-015 |
### Section
