### Hypotheses
(…)
| hypothesis |
| 1 | незнакомый параметр маршрута | да, живьём | `/agents/nope`, `/containers/nope`, `/sites/nope` → честный 404, но страница 404 английская и белая (**H7-006**). `/agents/%zz` → **500** (**H7-003**) |
| 2 | параметр с пробелами и скобками | да, живьём | `<host> [заглушка]` открывается и справа, и ссылкой с главной; двойного раскодирования нет. Дефекта нет — подробности ниже |
| 3 | узкий экран 390 | да, снимками всех шести страниц | главная и журнал не переживают (**H7-001**, **H7-002**, **H7-014**); страницы агента, контейнера, сайта и долгов переживают |
| 4 | что уехало в браузер | да | ничего лишнего: значения секретов замаскированы сборщиком, в разметке ни одного немаскированного. Подробности ниже |
| 5 | `tip-layer` перекрывает нажатие | да | не перекрывает (`pointer-events: none`), но подсказка залипает у края (**H7-007**) |
| 6 | клавиатура | да, по выдаче сервера | 196 из 296 триггеров подсказок недостижимы с клавиатуры (**H7-008**); фокус в поиске и фильтрах не виден (**H7-009**); окно иконки не закрыть (**H7-010**) |
| 7 | подписи мимо i18n | да, сплошным просмотром на кириллицу вне комментариев | три места (**H7-012**, **H7-013**, часть **H7-016**) |
| 8 | логотип и шрифты | да | шрифты системные, ни одного запроса за гарнитурой; css-пакет 58 КБ (≈10 КБ сжатым), boilerplate shadcn вычищен tailwind v4 — **дефекта нет**. Зато логотип не рисуется вовсе (**H7-011**) |
## Section
(…)
- **Пробелы и скобки в параметре (гипотеза 2) — дефекта нет.** В фактах живёт
(…)
- **Что уехало в браузер (гипотеза 4) — дефекта нет.** Прошёл все 257 строк
(…)
- **Шрифты и вес (гипотеза 8) — дефекта нет.** Ни одного запроса за гарнитурой:
