### Hypotheses
(…)
| hypothesis |
| 1 | `just lint` не работает | запуск `just lint`; прогон `eslint` и разбор его вывода | подтверждена → H8-101 |
| 2 | `just boundaries` ловит импорт вверх | **эксперимент**: копия дерева, заведомо неверный импорт `shared → widgets`, прогон всех ворот | подтверждена → H8-102 |
| 3 | `just ci` против CI | поиск любых конфигураций CI в индексе git | подтверждена → H8-104 |
| 4 | запуск на машине мимо `mise` | чтение юнита, сверка с `mise.toml` и деревом `installs/node` | подтверждена → H8-111 |
| 5 | `mise.toml` и доверие | воспроизведена ошибка недоверенного конфига; поиск упоминаний в документации | частично: ошибка воспроизведена, упоминания нет нигде → см. «Проверено» |
| 6 | документация врёт | сверка `AGENTS.md`, `README.md`, `REVIEW.md`, `invariants.md` с кодом и машиной | подтверждена → H8-103, H8-104, H8-106, H8-107, H8-108, H8-113 |
| 7 | зависимости | разбор `package.json`, `pnpm-workspace.yaml`, проверка загрузки `sharp` | частично → H8-112, H8-114 |
| 8 | `next.config.ts` и заголовки | чтение конфигурации, проверка манифеста предрисовки; **живые заголовки снять не удалось** — служба на `127.0.0.1:80` из песочницы недоступна | частично: предрисовка проверена и корректна, заголовки не проверялись |
| 9 | тест проходит на сломанном коде | **эксперимент**: проверки из HEAD прогнаны на исходниках коммита `95536e4` (до первой починки H1) | опровергнута: 46 из 59 краснеют → см. «Проверено» |
| 10 | тест проверяет сам себя | чтение `fixtures.mts` и всех проверок | в основном опровергнута, одно место → H8-115 |
| 11 | проверено только запрещённое | пересчёт обратных проверок | опровергнута → см. «Проверено» |
| 12 | исполнитель тестов не в воротах | чтение `justfile:79` | устарела: `test` в воротах стоит, но до него не доходит — H8-101 |
### Section
(…)
**Проверки (`tests/`) не врут — доказано прогоном на старом коде.** Гипотеза 9
(…)
**Обратных проверок достаточно — гипотеза 11 не подтвердилась.** Их не «не меньше»,
(…)
**Образцы не готовятся проверяемым кодом — гипотеза 10 в основном не
