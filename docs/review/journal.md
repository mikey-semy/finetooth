# Review journal

- **2026-09-24T13:09:14Z** · `T1` — Review of the kit itself set up: four blocks (T1 tool, T2 prompts, T3 tests as a measured gate, T4 repository contract), 70/70 files owned, invariants from AGENTS/CONTRIBUTING/SECURITY, manifest T1 with 15 hypotheses. First run planned after the weekly quota resets (Sun 27.09): assets/run-role.sh T1 hunter — also the live test of the runner and the economy rules.
- **2026-09-24T14:41:06Z** · `T1` — hunter — spend: 17 min, 45 turns, 44 tool calls, input 3.8M tokens (94% from cache, 240k written), output 83k, re-reads 0, cost estimate $6.26, model claude-opus-5
- **2026-09-24T15:20:56Z** · `T1` — verify — spend: 38 min, 50 turns, 49 tool calls, input 8.6M tokens (97% from cache, 291k written), output 101k, re-reads 0, cost estimate $9.60, model claude-opus-5
- **2026-09-24T15:21:34Z** · `T1` — Accepted against the criterion: all three tables present (gate → test → mutation measured by 39 mutants: 24 gates survive removal; MSG en/ru — 56 keys each, no difference; parser matrix 30 lines executed). 24 findings confirmed by execution (1 high, 12 medium, 11 low), none rejected; verifier corrected five hunter claims (T1-007 14→24 untested gates; T1-011 downgraded; T1-019 raised; T1-014 narrowed) and added T1-022..024. The remaining check reds are the findings themselves: the hunter report trips five verdict conflicts (T1-001 class, live), and three roots with ≥3 instances need guards — work for the fix phase. Economy measured: hunter 45 turns/17 Bash (H3: 55/33), verifier 50 turns/25 Bash (H3: 165/118), $6.26 + $9.60 vs $7.03 + $18.15; the stand-as-one-script rule cut the verifier by 3×. Hunter allowlist lacks python3 — on a Python project it cannot execute anything; fix in run-role.sh.
- **2026-09-24T15:22:59Z** · `T1` — The fix prompt refused to assemble: findings quote {{FILES}}/{{HUNTER_NOTE}}, and the placeholder check ran on the assembled text. Fixed by the lead (check the template, not the result), with a test and a mutation, before the fixer starts — a blocker of the fix phase, not a finding of the block.
- **2026-09-24T16:43:12Z** · `T1` — Fix phase closed: all 24 findings fixed, none rejected; 10 commits 242f88b…f90c68d, the suite green at every one of them (205 tests). Proven by mutation twice: 62 of 62 gates of check go red when silenced, and every fix goes red when reverted, with the build intact in each mutant. Three classes closed by guards recorded on the findings — parser shape → ReportShapeTest, a gate that cannot go red → GateRegistryTest, hand-edited input → HandWrittenInputTest; the -z rule and the constants rule live in SourceRuleTest. Two halves deliberately left undone and written up in the fix report: a bare 'verified' in prose (T1-003) cannot be told from a legitimate verdict without refusing honest reports, and 'все находки проверены' (T1-019) is the Russian of the sentence the same finding forbids. Left for acceptance: the hunter report's own six contradictory verdicts (identical before and after the parser fix — a report is not code, and the fixer does not rewrite it) and restamp T1 after the diff review. Version 0.8.0 with a Breaking section: verdicts inside a fence and the named-files substring no longer pass.
- **2026-09-24T16:47:04Z** · `T1` — fix — spend: 0 min, 2 turns, 329 tool calls, input 112.9M tokens (100% from cache, 528k written), output 1k, re-reads 9, cost estimate $67.92, model claude-opus-5
- **2026-09-24T17:19:05Z** · `T1` — fixreview — spend: 24 min, 80 turns, 79 tool calls, input 16.6M tokens (98% from cache, 285k written), output 81k, re-reads 0, cost estimate $15.08, model claude-opus-5
- **2026-09-24T17:50:05Z** · `T1` — Fix phase round 2 (the findings of fix review round 1): all 7 findings fixed, none rejected; commits 6ae9c90…341c56d, the suite green at the end of the series (217 tests) and skills-ref valid. Every finding reproduced in the current code before it was touched, and every fix proven by reverting it with the build intact. Two classes closed by guards recorded on the findings: the records of git log -z are parsed in one place (SourceRuleTest.test_записи_коммитов_разбираются_одним_местом) and recognising a quotation lives in one place that every markdown parser calls (SourceRuleTest.test_цитаты_распознаются_одним_местом, which replaces the count-based fence rule the previous round's review called weaker than it reads). The parser change moves no verdict of this review: verdict_mentions is byte-identical on all four T1 reports and the manifest before and after. New finding named in the report but not fixed (a different kind, not a fix inside someone else's): import accepts a claim or scenario over the limit that check then refuses, with nothing said at import time. Left for acceptance, as in round 1: restamp T1 and the hunter report's own six contradictory verdicts.
- **2026-09-24T17:54:16Z** · `T1` — fix — spend: 34 min, 186 turns, 185 tool calls, input 28.7M tokens (99% from cache, 226k written), output 106k, re-reads 0, cost estimate $19.18, model claude-opus-5
- **2026-09-24T18:12:17Z** · `T1` — fixreview — spend: 13 min, 63 turns, 62 tool calls, input 8.2M tokens (98% from cache, 168k written), output 61k, re-reads 0, cost estimate $7.28, model claude-opus-5
- **2026-09-24T18:43:43Z** · `T1` — Fix phase round 3 (the findings of fix review round 2): all 5 findings fixed, none rejected; commits 74c7522…4ef5d7f, the suite green at the end of the series (230 tests, 13 added) and skills-ref valid. Every finding reproduced in the current code before it was touched, and every fix proven by reverting it with the build intact (ast.parse for review.py, bash -n for run-role.sh), one mechanism at a time where a fix had two. The quotation class produced a fix in all three rounds of this block, so its guard no longer works off a list of parser names: SourceRuleTest.test_цитаты_распознаются_одним_местом finds a markdown parser by its shape, and test_узда_видит_разборщика_которого_ещё_нет holds both sides of that rule on five synthetic sources. The rule was measured red on three parsers that do not exist in the tool and on one that does — acceptance_of pasted a fenced example into the summary's cell, a place no finding named; it is fixed with its own test. Also fixed along the way: the unreleased changelog bullet naming fenced_lines, which no longer exists. The parser change moves no verdict of this review: verdict_mentions and section_items are identical on all eight T1 documents before and after. Left for acceptance, as in the two rounds before: restamp T1 and the hunter report's own six contradictory verdicts. Still open as a finding of its own from round 2: import accepts a claim or scenario over the limit that check then refuses — that is how T1-033's 757-character scenario reached the register, trimmed here as bookkeeping.
- **2026-09-24T18:48:33Z** · `T1` — fix — spend: 35 min, 220 turns, 219 tool calls, input 42.0M tokens (99% from cache, 266k written), output 118k, re-reads 2, cost estimate $26.47, model claude-opus-5
